<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>hello</h1>
    <h1><%= question.title%></h1>
    <h2><%= question.question%></h2>
    <p><%= question.difficult%></p>
    <code><%= question.code%></code>
   
    <a href="/questions/<%= question._id %>/edit">Edit</a>

    <p id="counter">Loading button click data.</p>
    <button id="myButton">Click me!</button>

    <% question.answer.forEach(function(a) { %>
        <p><%= a.content %></p>
        <code><%= a.code %></code>
        <a href="/users/<%= a._id %>/edit">Edit</a>
    <% }) %>
    <form method="POST"  action="/questions/<%= question._id %>/answer">
        <label>Answer</label>
        <textarea name="content">

        </textarea>
        <label>Solution Code</label>
        <textarea name="code">

        </textarea>
        <input type="submit" value="asnwer">
    </form>
</body>
</html>